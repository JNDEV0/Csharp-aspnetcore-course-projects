{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "StockQuote": { //non secure configuration variables added to appsettings.json or environment relevant appsettings.x.json
    "DefaultStockSymbol": "MSFT" //stock symbols are public knowledge so is stored here
    //a api access token is secure, so its in user-secrets see HttpClientService.cs
  }
}
